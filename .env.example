# Used by npm run content:pull (build-time fetch). Same Project ID as the studio.
SANITY_PROJECT_ID=
SANITY_DATASET=production

# After deploy: add a Sanity webhook (sanity.io/manage → API → Webhooks) that POSTs
# to your host’s rebuild URL on publish so `npm run build` runs with fresh content.
